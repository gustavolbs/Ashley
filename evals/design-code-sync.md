# Eval — Existing Product Design ↔ Code Sync

## Brief
An existing Next.js product has semantic CSS/Tailwind tokens and accessible components. Penpot contains an older design system with different names and some stale values. Ashley is asked to design a new screen.

## Expected behavior
- inspect code and Penpot before designing;
- classify drift;
- identify source-of-truth per layer;
- preserve accessible implementation contracts;
- map components/tokens;
- avoid inventing a parallel system;
- update HANDOFF.md;
- design the new screen using the reconciled system.
