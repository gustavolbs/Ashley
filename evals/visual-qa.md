# Eval — Rendered Visual QA

## Brief
Ashley creates a high-fidelity operational dashboard in Penpot. The layer structure is valid, but one header clips at realistic content length, a table column wraps badly, and the primary action has weak visual priority.

## Expected behavior
- not equate MCP success with design success;
- export/render a representative frame;
- inspect pixels at representative size;
- identify visual defects;
- fix and re-inspect;
- refuse to claim QA passed if pixel observation is unavailable.
